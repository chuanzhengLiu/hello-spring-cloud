spring:
  application:
    name: hello-spring-cloud-config
  cloud:
    config:
      label: master
      server:
        git:
          #uri: https://github.com/topsale/spring-cloud-config
          uri: https://github.com/chuanzhengLiu/spring-cloud-config.git
          search-paths: respo
          username:root
          password:123456

server:
  port: 8888

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/